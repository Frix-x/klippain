## Auxiliary fan control macros, used in conjunction with config/hardware/fans/aux_fan.cfg
## The aux fan is an independent secondary part cooling fan, controllable by:
##   - START_AUX_FAN / STOP_AUX_FAN macros
##   - SET_FAN_SPEED FAN=aux_fan
##   - OrcaSlicer "Auxiliary Part Cooling Fan" via the M106 P2 / M107 P2 bridge
##
## Fallback M106/M107 macros below route P2 (aux fan) automatically for all configurations.
## When fan_bridge.cfg is also loaded (via part_fan.cfg), bare M106/M107 (P0) are forwarded
## to the native part fan via M106.1/M107.1. When using a toolhead board template instead of
## part_fan.cfg, you must include fan_bridge.cfg to enable this forwarding. Klippain validates
## this at startup and raises an error if the configuration is misconfigured.

[gcode_macro START_AUX_FAN]
description: Turn on the auxiliary fan at a given speed (SPEED as 0.0 - 1.0)
gcode:
    {% set SPEED = params.SPEED|default(1)|float %}
    {% set verbose = printer["gcode_macro _USER_VARIABLES"].verbose|default(False) %}
    {% set aux_fan_name = printer["gcode_macro _USER_VARIABLES"].aux_fan_name|default("aux_fan") %}

    SET_FAN_SPEED FAN={aux_fan_name} SPEED={SPEED}
    {% if verbose %}
        RESPOND MSG="Auxiliary fan set to {SPEED * 100}%"
    {% endif %}


[gcode_macro STOP_AUX_FAN]
description: Turn off the auxiliary fan
gcode:
    {% set verbose = printer["gcode_macro _USER_VARIABLES"].verbose|default(False) %}
    {% set aux_fan_name = printer["gcode_macro _USER_VARIABLES"].aux_fan_name|default("aux_fan") %}

    SET_FAN_SPEED FAN={aux_fan_name} SPEED=0
    {% if verbose %}
        RESPOND MSG="Auxiliary fan off"
    {% endif %}


# Fallback M106/M107 bridge for OrcaSlicer P2 routing.
# These are overridden by fan_bridge.cfg when it is loaded via part_fan.cfg.
# P2 commands from OrcaSlicer are routed to the auxiliary fan, and all other
# commands are forwarded to M106.1/M107.1 if fan_bridge.cfg is loaded.
[gcode_macro M106]
description: Set fan speed (OrcaSlicer compatible, P2 = auxiliary fan)
gcode:
    {% set aux_fan_enabled = printer["gcode_macro _USER_VARIABLES"].aux_fan_enabled|default(False) %}
    {% set aux_fan_name = printer["gcode_macro _USER_VARIABLES"].aux_fan_name|default("aux_fan") %}
    {% set fan_index = params.P|int if params.P is defined else 0 %}

    {% if fan_index == 2 and aux_fan_enabled %}
        {% set speed = (params.S|float if params.S is defined else 255) / 255 %}
        SET_FAN_SPEED FAN={aux_fan_name} SPEED={speed}
    {% elif "M106.1" in printer.gcode.commands %}
        M106.1 {rawparams|trim}
    {% endif %}


[gcode_macro M107]
description: Turn off fans (OrcaSlicer compatible, P2 = auxiliary fan)
gcode:
    {% set aux_fan_enabled = printer["gcode_macro _USER_VARIABLES"].aux_fan_enabled|default(False) %}
    {% set aux_fan_name = printer["gcode_macro _USER_VARIABLES"].aux_fan_name|default("aux_fan") %}
    {% set fan_index = params.P|int if params.P is defined else 0 %}

    {% if fan_index == 2 and aux_fan_enabled %}
        SET_FAN_SPEED FAN={aux_fan_name} SPEED=0
    {% elif "M107.1" in printer.gcode.commands %}
        M107.1
    {% endif %}


[delayed_gcode _STOP_AUX_FAN_DELAYED]
gcode:
    {% set aux_fan_tach_enabled = printer["gcode_macro _USER_VARIABLES"].aux_fan_tach_enabled|default(False) %}
    STOP_AUX_FAN
    {% if aux_fan_tach_enabled %}
        UPDATE_DELAYED_GCODE ID=_BACKGROUND_AUX_TACHO_CHECK DURATION=0
    {% endif %}


# Monitoring loop for the auxiliary fan safety check (only used with a tachometer)
[delayed_gcode _BACKGROUND_AUX_TACHO_CHECK]
gcode:
    {% set tach_poll_interval = printer["gcode_macro _USER_VARIABLES"].aux_fan_tach_poll_interval|default(3)|float %}
    _AUX_FAN_CHECK
    UPDATE_DELAYED_GCODE ID=_BACKGROUND_AUX_TACHO_CHECK DURATION={tach_poll_interval}

[gcode_macro _AUX_FAN_CHECK]
description: Check the auxiliary fan tachometer to verify that it is running effectively
variable_aux_stop_count: 0
gcode:
    {% set tach_poll_interval = printer["gcode_macro _USER_VARIABLES"].aux_fan_tach_poll_interval|default(3)|float %}
    {% set max_consecutive_stops = printer["gcode_macro _USER_VARIABLES"].aux_fan_tach_max_consecutive_stops|default(3)|int %}
    {% set aux_fan_name = printer["gcode_macro _USER_VARIABLES"].aux_fan_name|default("aux_fan") %}
    {% set aux_fan = "fan_generic " ~ aux_fan_name %}
    {% set rpm = printer[aux_fan].rpm|float %}
    {% set aux_target = printer[aux_fan].speed|float %}
    # Scale the minimum RPM threshold proportionally with the target fan speed.
    # At full speed the threshold is aux_fan_tach_min_rpm_max; at low soak speeds it drops toward
    # a floor of aux_fan_tach_min_rpm_floor so that intentionally slow fans don't false-positive.
    {% set min_rpm_max = printer["gcode_macro _USER_VARIABLES"].aux_fan_tach_min_rpm_max|default(1000)|float %}
    {% set min_rpm_floor = printer["gcode_macro _USER_VARIABLES"].aux_fan_tach_min_rpm_floor|default(200)|float %}
    {% set min_rpm = [min_rpm_max * aux_target, min_rpm_floor]|max %}
    {% set aux_stop_count = printer["gcode_macro _AUX_FAN_CHECK"].aux_stop_count|int %}

    {% if (aux_target > 0) and (rpm < min_rpm) %}
        SET_GCODE_VARIABLE MACRO=_AUX_FAN_CHECK VARIABLE=aux_stop_count VALUE={aux_stop_count + 1}
        RESPOND MSG="Auxiliary fan stoppage detected for {(aux_stop_count+1)*tach_poll_interval|int}sec (max {max_consecutive_stops*tach_poll_interval|int}sec allowed, min_rpm={min_rpm|int})"
        M400
        {% if printer["gcode_macro _AUX_FAN_CHECK"].aux_stop_count|int >= max_consecutive_stops - 1 %}
            CANCEL_PRINT
        {% endif %}
    {% else %}
        SET_GCODE_VARIABLE MACRO=_AUX_FAN_CHECK VARIABLE=aux_stop_count VALUE=0
    {% endif %}

